# titulo: Tipagem inferida de :Performance (domínio de :performer / :performanceIn)
# pergunta: Quantas atuações estão registradas?
# sql: SELECT COUNT(*) AS n FROM roles
PREFIX : <http://www.example.org/imdb#>

SELECT (COUNT(DISTINCT ?p) AS ?n)
WHERE { ?p a :Performance }
